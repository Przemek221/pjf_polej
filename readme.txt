Simple social media app using django framework.

SETUP:

1. Open terminal in your project root directory and execute below commands:
 - Install all required packages:
    pip install -r requirements.txt
 - Create database tables:
    python manage.py migrate
 - If you want to have superuser:
    python manage.py createsuperuser

2. Run the project.